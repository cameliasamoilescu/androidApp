package com.example.bazededate;

import android.os.AsyncTask;

public class    GetInfoForLocation extends AsyncTask<String, Void, String> {
    @Override
    protected String doInBackground(String... strings) {
        return "";
    }
}
